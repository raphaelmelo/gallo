import styled from 'styled-components'

const Container = styled.section`
  padding:0 10vw;
`

export default function Logo() {
    return (
        <Container>
            <h1>Logo</h1>
        </Container>
    );
}
